using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StaticHuman : Human
{
    //public StaticHuman(string prefabPath, Vector3 startPosition, float viewRadius, float viewAngle, float screamingRadius)
    //    : base(prefabPath, startPosition, viewRadius, viewAngle, screamingRadius)
    //{

    //}
    void Start()
    {
        base.Start();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
